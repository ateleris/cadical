name: Build cadical binaries

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - dev
    paths:
      - src/**
      - .github/workflows/build.yaml
jobs:
  build_linux_binary:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Build
        run: ./configure && make

      - name: List files
        run: ls -la build
